spring:
  application:
    name: ecommerce-app-demo
  datasource:
    url: jdbc:h2:mem:ecommerceapp;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS ecommerceapp
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    locations: classpath:db/migration/h2
